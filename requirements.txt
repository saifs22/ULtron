# ULTRON — Dependencies
# Install: pip install -r requirements.txt

# Core
anthropic>=0.39.0
python-dotenv>=1.0.0
pyyaml>=6.0
colorama>=0.4.6
rich>=13.0

# System Monitoring & Control
psutil>=5.9.0
pyautogui>=0.9.54
pygetwindow>=0.0.9
pyperclip>=1.8.2
mss>=9.0.0

# Notifications
plyer>=2.1.0

# Audio Control (Windows)
pycaw>=20230407
comtypes>=1.3.0

# Web Search
duckduckgo-search>=6.0.0
requests>=2.31.0

# File Safety
send2trash>=1.8.0

# Voice — Text-to-Speech (offline)
pyttsx3>=2.90

# Voice — Speech-to-Text (local, runs on CPU)
faster-whisper>=1.0.0
sounddevice>=0.4.6

# Voice — Hotkey listener (for push-to-talk)
pynput>=1.7.6

# Voice — Audio playback (for ElevenLabs TTS)
pygame>=2.5.0

# Optional: Premium TTS (uncomment if you have an ElevenLabs API key)
# elevenlabs>=1.0.0

# Optional: Tavily search (uncomment if you have an API key)
# tavily-python>=0.4.0

# Optional: Ollama local LLM (no pip install needed — just run Ollama)
